面向异构离线口令恢复任务的智能化安全评测系统

## 摘要

在密码技术竞赛和实际密码安全评测场景中，口令恢复任务通常具有明显的异构性，包括加密文档、压缩文件、快速Hash、慢速口令派生Hash以及批量关联目标等。

传统工具如 Hashcat、John the
Ripper（JtR）具备优秀的底层计算能力，但实际任务处理中仍依赖人工经验完成：

-   目标类型判断；
-   密码结构分析；
-   策略选择；
-   参数调整；
-   中间结果分析。

本项目提出 SAGE-Pass 智能化口令安全评测与自适应策略编排系统。

系统不替代底层密码计算工具，而是在其上构建智能决策层，通过：

1.  异构密码任务统一表示模型 PRIR；
2.  密码空间与验证成本分析；
3.  LLM辅助任务理解；
4.  规则约束可信决策；
5.  基于Bandit的自适应策略调度；
6.  恢复结果反馈学习；

实现有限时间和计算预算下的自动化策略优化。

------------------------------------------------------------------------

# 1 项目背景

## 1.1 研究背景

随着密码保护技术的发展，密码安全评测任务越来越复杂。

任务对象可能包括：

  类型       示例
  ---------- ------------------------
  加密文档   PDF、Word、Excel
  压缩文件   ZIP、RAR、7z
  快速Hash   MD5、SHA系列
  慢速Hash   bcrypt、PBKDF2、Argon2
  批量任务   大量Hash或文件集合

不同任务具有不同约束：

-   时间限制；
-   候选验证次数限制；
-   GPU资源限制；
-   不同验证成本；
-   多目标关联关系。

传统流程：

    人工分析
     ↓
    判断目标类型
     ↓
    选择策略
     ↓
    运行工具
     ↓
    分析结果
     ↓
    调整策略

该模式存在：

-   自动化程度不足；
-   依赖专家经验；
-   无法动态调整；
-   计算资源利用效率低。

------------------------------------------------------------------------

# 2 项目目标

本项目目标不是重新实现密码恢复算法，而是设计一个智能化策略编排平台。

核心问题：

> 面对未知类型、有限预算、动态反馈的口令安全评测任务，系统如何自动选择当前最有效的评测策略？

系统目标：

    自动识别任务

    ↓

    建立密码任务画像

    ↓

    生成候选策略

    ↓

    智能分配预算

    ↓

    调用底层工具

    ↓

    根据结果重新规划

------------------------------------------------------------------------

# 3 总体架构

                     输入任务

                        |

                 Task Analyzer

                        |

                  PRIR Model

                        |

           Intelligent Planner

        LLM + Rule + Scheduler

                        |

                 Strategy Pool

                        |

              Executor Adapter

           Hashcat / JtR / Generator

                        |

               Feedback Engine

                        |

               Knowledge Base

------------------------------------------------------------------------

# 4 PRIR统一任务表示模型

## 4.1 设计思想

不同输入对象：

-   PDF；
-   ZIP；
-   Hash；

底层完全不同。

但是对于智能调度而言，都可以抽象为：

-   类型；
-   算法；
-   成本；
-   搜索规模；
-   上下文信息；
-   历史结果。

因此提出：

Password Recovery Intermediate Representation。

## 4.2 数据结构

示例：

``` json
{
"id":"T001",
"type":"hash",
"algorithm":"bcrypt",
"salt":true,
"verification_cost":"high",
"context_available":true,
"candidate_space":100000000,
"budget":3600
}
```

------------------------------------------------------------------------

# 5 密码安全特征分析

## 5.1 候选空间估计

定义：

\[ N=\|`\Sigma`{=tex}\|\^L \]

其中：

-   Sigma表示字符集合；
-   L表示密码长度。

用于：

-   判断暴力搜索可行性；
-   选择概率策略；
-   估计计算成本。

------------------------------------------------------------------------

## 5.2 验证成本模型

定义：

\[ Cost=N`\times `{=tex}T\_{verify} \]

其中：

-   N为候选数量；
-   Tverify为单次验证成本。

不同算法：

  类型      特点
  --------- ------------
  MD5/SHA   验证快
  PBKDF2    成本较高
  bcrypt    慢速Hash
  Argon2    高成本算法

调度器根据成本动态分配资源。

------------------------------------------------------------------------

# 6 Strategy Pool设计

系统将策略抽象为：

    Strategy =
    Candidate Generator

    +

    目标规则

    +

    预算限制

    +

    停止条件

## S1 Baseline

基础高频候选。

## S2 Rule

规则变换：

-   大小写变化；
-   数字添加；
-   符号替换。

## S3 PCFG-lite

密码结构概率模型。

例如：

    Word + Year

    Name + Number

    Capital + Digit

## S4 Context Strategy

利用任务提供信息：

-   地区；
-   组织；
-   年份；
-   关键词。

## S5 Transfer Strategy

利用历史恢复结果学习结构。

------------------------------------------------------------------------

# 7 LLM辅助智能规划模块

## 7.1 定位

LLM不是密码生成器。

负责：

-   理解背景；
-   提取语义；
-   推荐策略。

最终决策：

由Scheduler完成。

------------------------------------------------------------------------

## 7.2 可信架构

    LLM建议

    ↓

    Policy Validator

    ↓

    Adaptive Scheduler

    ↓

    执行

------------------------------------------------------------------------

## 7.3 稳定机制

### 固定输入

LLM只接收Task Profile。

### 固定输出

采用JSON Schema。

### 策略白名单

只能选择已有策略。

### 规则过滤

检查：

-   是否满足条件；
-   是否符合预算。

### 降级机制

LLM异常时：

切换规则规划器。

------------------------------------------------------------------------

# 8 Adaptive Scheduler设计

## 8.1 Bandit模型

定义：

目标组 + 策略

作为一个Arm。

维护：

    tested

    recovered

    time_cost

    success_rate

    recent_gain

------------------------------------------------------------------------

## 8.2 评分函数

\[ Score= w_1P\_{success} +w_2Gain +w_3Transfer -w_4Cost \]

选择最高评分策略。

------------------------------------------------------------------------

## 8.3 调度流程

    初始化策略

    ↓

    小规模探索

    ↓

    统计收益

    ↓

    调整权重

    ↓

    扩大高收益策略预算

------------------------------------------------------------------------

# 9 Feedback Engine

执行结果反馈：

-   恢复数量；
-   候选覆盖；
-   策略收益；
-   密码结构。

提取：

-   长度模式；
-   字符组合；
-   数字位置；
-   后缀模式。

形成知识：

Pattern Knowledge。

------------------------------------------------------------------------

# 10 Executor执行适配层

系统不重新开发计算引擎。

通过接口调用：

-   Hashcat；
-   JtR；
-   自定义生成器。

接口：

``` python
execute(
strategy,
target,
budget
)
```

返回：

``` json
{
"time":100,
"tested":1000000,
"recovered":5
}
```

------------------------------------------------------------------------

# 11 软件结构设计

    sage-pass/

     analyzer/

     model/

     planner/

     scheduler/

     strategy/

     executor/

     feedback/

     database/

     frontend/

------------------------------------------------------------------------

# 12 实验设计

## 实验1：整体性能

对比：

-   固定策略；
-   SAGE-Pass。

指标：

-   恢复数量；
-   时间；
-   单位预算收益。

------------------------------------------------------------------------

## 实验2：消融实验

分别去除：

-   LLM；
-   Feedback；
-   Scheduler。

验证模块贡献。

------------------------------------------------------------------------

## 实验3：预算变化实验

测试：

-   10分钟；
-   30分钟；
-   60分钟。

------------------------------------------------------------------------

# 13 创新点总结

## 创新1

异构密码任务统一表示模型PRIR。

## 创新2

LLM辅助、规则约束、Bandit优化的可信智能规划架构。

## 创新3

基于恢复反馈的密码结构迁移机制。

## 创新4

面向有限预算的动态策略调度模型。

------------------------------------------------------------------------

# 14 与传统工具区别

  系统        特点
  ----------- --------------
  Hashcat     高速计算
  JtR         密码恢复
  传统脚本    固定流程
  SAGE-Pass   智能策略编排

核心区别：

> Hashcat负责计算，SAGE-Pass负责决定计算什么、什么时候计算以及为什么选择该策略。

------------------------------------------------------------------------

# 15 总结

SAGE-Pass通过：

-   统一任务建模；
-   密码特征分析；
-   LLM语义理解；
-   自适应策略调度；
-   反馈学习；

将传统人工经验驱动的密码安全评测流程转变为：

    自动感知

    智能规划

    动态执行

    持续优化

的智能化闭环系统。
